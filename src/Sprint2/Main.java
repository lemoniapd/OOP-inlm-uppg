package Sprint2;

public class Main {
    public static void main(String[] args) {
        new BestGymEver();
    }

/*
try (BufferedReader input = new BufferedReader(new FileReader(String.valueOf(p)))) {
        //int counter = 0;
        //int memberCounter = 0;
        String newLine = input.readLine().trim();
        memberList.add(new Member(newLine.substring(0, newLine.indexOf(',')),
        newLine.substring(newLine.indexOf(',') + 1)));
        memberList.get().setDateOfLastPayment(input.readLine());

        //System.out.println(memberList.get(0).getDateOfLastPayment());
        }
        //if (newLine == null) break;


            if (counter % 2 != 0) {
                memberList.add(new Member(newLine.substring(0, newLine.indexOf(',')),
                        newLine.substring(newLine.indexOf(',') + 1)));
                counter++;
            } else {
                memberList.get(memberCounter).setDateOfLastPayment(input.readLine());
                memberCounter ++;
                System.out.println(memberList.get(0).getDateOfLastPayment());
            }
            //if (newLine == null) break;



        catch (FileNotFoundException e) {
        JOptionPane.showMessageDialog(null, "Filen finns inte!");
        e.printStackTrace();
        System.exit(0);
        } catch (Exception e) {
        JOptionPane.showMessageDialog(null, "Någonting gick fel!");
        e.printStackTrace();
        System.exit(0);
        }
        */
}